import 'package:flutter/material.dart';

class WatchView extends StatelessWidget {
  const WatchView({Key? key,required this.isSeries}) : super(key: key);
final bool isSeries;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
